<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="SportPlus es una tienda online de deportes."/>
  <meta name="keywords" content="ecommerce, hombre, futbol, deporte, femenino, pelotas, basket, mujer, nike, adidas. "/>
  <meta name="author" content="lucas.m.albrigi@gmail.com">
  <meta name="publisher" content="lucas.m.albrigi@gmail.com">

  <title>{{title}}</title>
  <link rel="icon" type="image/x-icon" href="/img/favicon_io/favicon.ico" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  <link rel="stylesheet" href="/css/{{style}}" />
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css" />
</head>
<body>
  <div id="wrapper-navbar" class="sticky-top">
    <nav class="bg-body-tertiary fixed-top navbar navbar-expand-lg navbar-light">
      <div class="container-fluid d-flex justify-content-between align-items-center" style="background-color: black; box-shadow: 0 4px 10px -2px orange; ">
        <div class="nav-left col-3">
          
        </div>
        <div class="nav-center d-flex justify-content-center mx-auto col-6" style="color:black">
          <a  href="/">
            <h1 class="span" style="font-family:Antonio;padding: 0;margin: 0;">S<span>port</span> P<span>lus</span></h1>
          </a>
        </div>
        <div class="nav-right d-flex align-items-center col-3" style="justify-content: right;">
          <div class="nav-icons" style="display: flex; gap: .9rem; justify-content: flex-end;">
            <!-- Íconos aquí -->
          </div>
          {{#if user}}
            <div class="dropdown">
              <button class="btn border-0 dropdown-toggle" type="button" id="userMenuButton" data-bs-toggle="dropdown" aria-expanded="false" style="display: flex; align-items: center;" title="Perfil de usuario">
                <img src="{{avatar}}" id="profilePic" alt="Perfil" style="border-radius: 50%; width: 50px; height: 50px; object-fit: cover; margin-right: 10px;" />
                <span>
                  <strong style="color:#b14040; text-transform:capitalize">{{user.first_name}} {{user.last_name}}</strong>
                </span>
              </button>
              <ul class="dropdown-menu" aria-labelledby="userMenuButton">
                <li><a class="dropdown-item" href="/profile">Mi Perfil</a></li>
                <li hidden><a class="dropdown-item" href="/profile" id="user-id">{{user._id}}</a></li>
                <li hidden><a class="dropdown-item" href="/profile">✉ {{user.email}}</a></li>
                {{#if user.age}}
                  <li hidden><a class="dropdown-item" href="/profile">Edad: {{user.age}}</a></li>
                {{/if}}
                <li hidden> <a class="dropdown-item" href="/profile"></a>Id: <span id="user-id">{{user._id}}</span></li>
                <li><a class="dropdown-item" href="/profile">Cuenta: <span id="user-role">{{user.role}}</span></a></li>
                <li><hr class="dropdown-divider"></li>
                <li>
                  <form action="/api/sessions/logout" method="POST">
                    <button type="submit" class="dropdown-item">Cerrar sesión</button>
                  </form>
                </li>
              </ul>
            </div>
          {{else}}
            <div style="display: flex; justify-content: center; align-items: center;">
              <form action="/login" method="GET">
                <button type="submit" class="btn button-inicio destroySession">Iniciar sesión</button>
              </form>
            </div>
          {{/if}}
          {{#if user.cart}}
            <a id="carritoLink" class="carrito-text" href="/cart/{{user.cart._id}}">
              <img style="padding: 0px 5px 0px 0px" src="/img/imagen-carrito.svg" alt="Icono del carrito de compras"></img>
              <span id="numeroCarrito" class="numeroCarrito badge rounded-circle">{{totalQuantityInCart}}</span>
            </a>
          {{else}}
            <a id="carritoLink" class="carrito-text">
              <img style="padding: 0px 5px 0px 0px" src="/img/imagen-carrito.svg" alt="Icono del carrito de compras"></img>
            </a>
          {{/if}}
        </div>
      </div>
    </nav>
    <nav id="desktop-nav" class="d-none d-lg-flex navbar navbar-expand-lg navbar-light antiquewhite fixed-top" aria-labelledby="desktop-nav-label">
        <ul id="desktop-menu" class="navbar-nav mx-auto container justify-content-center uppercase">
          <li class="nav-link"><a href="/home" title="Ir a la página principal">Home</a></li>
          <li class="nav-link"><a href="/products?page=1" title="Ver todos los productos">Productos</a></li>
          {{#if userAdminOrPremium}}
            <li class="nav-link"><a href="/realtimeproducts" title="Gestionar productos">Productos en tiempo real</a></li>
          {{/if}}
          <li class="nav-link"><a href="/chat" title="Acceder al chat">Chat</a></li>
        </ul>
    </nav>
  </div>
  <div class="main-content">
    {{{body}}}
  </div>
  <br>
  <footer class="footer_style">
    <a href="https://www.facebook.com/Sport-Plus/" target="_blank" class="fa fa-facebook" aria-hidden="true" title="Facebook"></a>
    <a href="https://www.linkedin.com/Sport-Plus/" target="_blank" class="fa fa-linkedin" aria-hidden="true" title="LinkedIn"></a>
    <a href="https://www.instagram.com/Sport-Plus/" target="_blank" class="fa fa-instagram" aria-hidden="true" title="Instagram"></a>
  </footer>
  <!-- Scripts necesarios Fuentes y Bootstrap-->
  <script src="https://kit.fontawesome.com/4e9044019f.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>

<style>

.dropdown-menu {
  z-index: 1050;
  top: 100%;
  right: 0;
  left: auto
}

#desktop-nav {
  z-index: 1000;
}
.dropdown-item.active, .dropdown-item:active {
    background-color:black;
}

</style>